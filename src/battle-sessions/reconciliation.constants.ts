// 대조검증 허용 오차 — docs/battle-session-validation/spec.md 엣지 케이스 표.
// 순간적 네트워크 단절로 킬 배치 몇 건이 재시도로도 유실된 정상 플레이를
// 오탐하지 않기 위한 여유 마진(save-sync FR-7 상식 검증의 "여유 마진" 선례).
// 목적은 밸런스 검증이 아니라 "이벤트 로그로 설명되지 않는 대량 조작" 차단이므로
// 제출값이 로그 합계를 이 값 이상 초과할 때만 불일치로 판정한다.
// 반대 방향(제출값 < 로그 합계)은 유실이 아니라 정상적으로 발생할 수 없지만,
// 리더보드에 유리하지 않은 방향이라 판정하지 않는다.
export const KILL_TOLERANCE = 5;
export const GOLD_TOLERANCE = 5;
